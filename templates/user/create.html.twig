{% extends 'base.html.twig' %}

{% block header_title %}
	<h1>Créer un utilisateur</h1>
{% endblock %}
{% block header_img %}{% endblock %}

{% block body %}
	<div class="row">
		{{ form_start(form, {'action' : path('user_create')}) }}
		{{ form_widget(form) }}
		<div class="d-flex justify-content-end">
			<button type="submit" class="btn btn-success my-1">Ajouter</button>
		</div>
		{{ form_end(form) }}
	</div>
{% endblock %}
